{
	"dependencies": {
		"@types/chrome": "^0.0.268",
		"ts-loader": "^9.5.1",
		"typescript": "^5.4.5"
	},
	"scripts": {
		"copy-src-files": "copyfiles -u 1 src/**/*.html src/**/*.css src/**/*.js src/**/*.json target",
		"copy-icons": "copyfiles icons/* target",
		"copy-markdown": "copyfiles CHANGELOG.md LICENSE.md README.md target",
		"copy-manifest": "copyfiles manifest.json target",
		"copy-all": "npm run copy-src-files && npm run copy-icons && npm run copy-markdown && npm run copy-manifest",
    "build": "tsc && npm run copy-all"
  }
}
